import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    text: {
      primary: '#fff'
    },
    background: {
      default: '#08081D'
    }
  },

  typography: {
    fontFamily: '"acumin-pro", sans-serif',
  }
})